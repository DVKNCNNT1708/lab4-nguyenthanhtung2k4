name: Lab04 Docker and Newman Verification

on:
  push:
  pull_request:

jobs:
  docker-newman:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Node dependencies
        run: npm ci

      - name: Lint OpenAPI contract
        run: npm run lint:openapi

      - name: Build Docker image
        run: docker build -t fit4110/team-vision:lab04 .

      - name: Run Docker container
        run: |
          docker run -d --rm \
            --name fit4110-team-vision-lab04 \
            -p 8000:8000 \
            --env-file .env.example \
            fit4110/team-vision:lab04

      - name: Wait for service health
        run: |
          for i in {1..30}; do
            if curl -fsS http://localhost:8000/api/v1/vision/health; then
              exit 0
            fi
            echo "Waiting for TeamVision health ($i/30)..."
            docker ps -a
            docker logs fit4110-team-vision-lab04 || true
            sleep 2
          done
          echo "TeamVision health endpoint did not become ready"
          docker ps -a
          docker logs fit4110-team-vision-lab04 || true
          exit 1

      - name: Run Newman against Docker container
        run: npm run test:local

      - name: Show container logs on failure
        if: failure()
        run: docker logs fit4110-team-vision-lab04 || true

      - name: Stop container
        if: always()
        run: docker stop fit4110-team-vision-lab04 || true

      - name: Upload Newman reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lab04-newman-reports
          path: reports/
